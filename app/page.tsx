import {
  PageActions,
  PageHeader,
  PageHeaderDescription,
  PageHeaderHeading,
} from '@/components/page-header';
import Pager from '@/components/pager';
import { Button } from '@/components/ui/button';
import { siteConfig } from '@/config/site';
import { ExternalLink, Mail } from 'lucide-react';
import Link from 'next/link';

const IntroductionPage = async () => {
  return (
    <>
      <PageHeader>
        <PageHeaderHeading>Azka Shaukat</PageHeaderHeading>
        <PageHeaderHeading className="mt-2 text-muted-foreground">
          Engineering Intelligence. Building Scalable Systems.
        </PageHeaderHeading>
        <PageHeaderDescription>
          I am an AI/ML Engineer and Full-Stack Developer with a BS in Software Engineering (CGPA: 3.85). 
          I specialize in bridging the gap between raw data algorithms and production-ready applications, 
          leveraging PyTorch, FastAPI, React, and local LLMs to solve complex problems and deliver intelligent software.
        </PageHeaderDescription>
        <PageActions>
          <Button asChild size="sm" className="rounded-md">
            <Link href={siteConfig.links.resume} target="_blank">
              Get Resume
              <ExternalLink className="size-3" strokeWidth={2} />
            </Link>
          </Button>
          <Button asChild size="sm" variant="ghost" className="rounded-md">
            <Link href={siteConfig.links.email}>
              <Mail className="size-4" />
              Send Mail
            </Link>
          </Button>
        </PageActions>
      </PageHeader>

      <Pager
        prevHref="/"
        nextHref="/about"
        prevTitle="Previous"
        nextTitle="About Me"
      />
    </>
  );
};
export default IntroductionPage;
